import { Galindo, Montserrat } from 'next/font/google'

export const galindo = Galindo({
  variable: '--font-galindo',
  subsets: ['latin'],
  weight: '400',
})

export const montserrat = Montserrat({
  variable: '--font-montserrat',
  subsets: ['latin'],
  weight: 'variable',
})
